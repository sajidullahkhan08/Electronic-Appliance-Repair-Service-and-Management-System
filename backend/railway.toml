# ElectroFix — Railway Deployment Configuration
# This file tells Railway how to build and start the backend.

[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn app:app --bind 0.0.0.0:$PORT --workers 2 --timeout 120"
healthcheckPath = "/"
restartPolicyType = "on_failure"
